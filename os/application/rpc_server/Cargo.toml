[package]
edition = "2024"
name = "rpc_server"
version = "0.1.0"
authors = ["Yannis Wagner <yannis.wagner@hhu.de>"]

[lib]
crate-type = ["staticlib"]
path = "src/rpc_server.rs"
test = false
doctest = false
bench = false

[dependencies]
# Local dependencies
runtime = { path = "../../library/runtime" }
rpc = { path = "../../library/rpc" }
terminal = { path = "../../library/terminal" }
log = "0.4.26"
logger = { path = "../../library/logger" }
spin = "0.9.8"
